from django.apps import AppConfig


class NewsConfig(AppConfig):
    name = 'news'
    def ready(self):
        # pass
        # from rss import deneme
        print()
        print("class NewsConfig(AppConfig):class NewsConfig(AppConfig):class NewsConfig(AppConfig):class NewsConfig(AppConfig):")
        # from rss_bundler import scrapyapschedular
        # from rss_bundler import runapscheduler
        # scrapy = scrapyapschedular.Command()
        # scrapy.handle()

        # rss = runapscheduler.Command()
        # rss.handle()




